<UserControl x:Class="FixFlow.TradeAllocBridge.WPF.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#ffffff">
    <UserControl.Resources>
        <CollectionViewSource x:Key="SettingsViewSource" Source="{Binding Settings}">
            <CollectionViewSource.GroupDescriptions>
                <PropertyGroupDescription PropertyName="Section"/>
            </CollectionViewSource.GroupDescriptions>
        </CollectionViewSource>
    </UserControl.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="App Settings"
                   FontSize="14"
                   FontWeight="SemiBold"
                   Foreground="#333"/>

        <TextBlock Grid.Row="1"
                   Margin="0,6,0,10"
                   FontSize="11"
                   Foreground="#666"
                   Text="{Binding StatusMessage}"/>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Source={StaticResource SettingsViewSource}}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  HeadersVisibility="Column"
                  FontSize="11"
                  GridLinesVisibility="Horizontal"
                  BorderBrush="#d0d0d0"
                  BorderThickness="1">
            <DataGrid.GroupStyle>
                <GroupStyle>
                    <GroupStyle.ContainerStyle>
                        <Style TargetType="{x:Type GroupItem}">
                            <Setter Property="Margin" Value="0,0,0,6"/>
                        </Style>
                    </GroupStyle.ContainerStyle>
                    <GroupStyle.HeaderTemplate>
                        <DataTemplate>
                            <Border Background="#f2f2f2" Padding="6" BorderBrush="#d0d0d0" BorderThickness="0,0,0,1">
                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" Foreground="#333"/>
                            </Border>
                        </DataTemplate>
                    </GroupStyle.HeaderTemplate>
                </GroupStyle>
            </DataGrid.GroupStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Key" Binding="{Binding DisplayKey}" IsReadOnly="True" Width="200"/>
                <DataGridTemplateColumn Header="Value" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding DisplayValue}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                    <DataGridTemplateColumn.CellEditingTemplate>
                        <DataTemplate>
                            <TextBox Text="{Binding EditValue, UpdateSourceTrigger=PropertyChanged}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellEditingTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Reload"
                    Command="{Binding ReloadCommand}"
                    Style="{StaticResource PrimaryButton}"
                    Background="#666666"
                    Height="32"
                    Margin="0,0,8,0"/>
            <Button Content="Save"
                    Command="{Binding SaveCommand}"
                    Style="{StaticResource PrimaryButton}"
                    Background="#107c10"
                    Height="32"/>
        </StackPanel>

        <TextBlock Grid.Row="4"
                   Text="FIX Transport Parameters"
                   FontSize="14"
                   FontWeight="SemiBold"
                   Foreground="#333"/>

        <TextBlock Grid.Row="5"
                   Margin="0,6,0,10"
                   FontSize="11"
                   Foreground="#666"
                   Text="{Binding FixDefaultsStatusMessage}"/>

        <DataGrid Grid.Row="6"
                  ItemsSource="{Binding FixDefaults}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  HeadersVisibility="Column"
                  FontSize="11"
                  GridLinesVisibility="Horizontal"
                  BorderBrush="#d0d0d0"
                  BorderThickness="1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Key" Binding="{Binding Key}" IsReadOnly="True" Width="200"/>
                <DataGridTextColumn Header="Value" Binding="{Binding Value, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</UserControl>
